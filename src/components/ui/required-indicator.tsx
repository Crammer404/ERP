import React from 'react'

/**
 * A visual indicator for required form fields.
 * Displays a red asterisk (*) to indicate that a field is mandatory.
 */
export function RequiredIndicator() {
  return <span className="text-red-600">*</span>
}

